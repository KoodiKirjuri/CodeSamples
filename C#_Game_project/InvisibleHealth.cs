/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InvisibleHealth : MonoBehaviour
{
    public GameObject myobject;
    public GameObject ChestMonster1;
    public bool activateme;
    private float healtit;

    // Update is called once per frame
    void Update()
    {
        healtit = GetComponentsInParent < EnenemyHealthManager.instance.EnemyCurrentHealth >
        ChestMonster1 = GameObject.Find("ChestMonster1");
        ChestMonster1.GetComponent(EnenemyHealthManager.instance.EnemyCurrentHealth);
        if (ChestMonster1.GetComponent<EnenemyHealthManager>().HurtEnemy(damageToTake) <= ChestMonster1.GetComponent<EnenemyHealthManager.instance.EnemyMaxHealth>)
        {

            myobject.SetActive(true);
         
        }
    }
}*/
